<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.example.shoppingmall.mapper.WishListMapper">
  <!-- 데이터 저장 -->
  <insert id="saveWishList" parameterType="Long">
    INSERT INTO wish_list(consumer_id, product_id) VALUES (#{consumerId}, #{productId})
  </insert>

</mapper>